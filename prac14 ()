/*Write a function that reads a file file1 and copies only alternative lines to another file file2. Alternative lines copied should be the odd numbered lines.Use Exception.*/


fopen = open("OLD.txt","r")
fwrite = open("NEW.txt","w")
oldfileonfo = fopen.readlines()
print(oldfileonfo)
for i in range(1,len(oldfileonfo),2):
    fwrite.write(oldfileonfo[i])
fwrite.close()
fopen.close()
